{
  "_doc": "Agent 定义候选改进注册表 schema。每次对定义文件的改进先登记为 pending，再走评估→审批→棘轮。字段：id(im-YYYYMMDD-NNN), target(目标文件路径), kind[FIX|DERIVED|REWRITE], signal{type[user-correct|metric|failure|manual], detail}, patch(diff 描述), status[pending|approved|rejected|reverted], created, score_before(可选), score_after(可选), evidence[]",
  "improvements": []
}
